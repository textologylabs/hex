# {{ project_name }}

{{ description }}

> Scaffolded from the `node-ts-hooked` reference component, which dogfoods
> Hex's M7 JS-hook sandbox: a `pre_render` hook logs the rendering context,
> and a `post_render` hook reads `package.json` through the sandboxed FS
> facade, optionally adding a `repository` field based on its own prompt.
